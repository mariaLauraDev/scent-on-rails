<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">Novo Lote</h1>

  <%= form_with model: @batch, local: false, class: "space-y-4" do |form| %>
    <%= render "form", form: form %>
    <div class="flex gap-4">
      <%= form.submit "Criar Lote", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to "Cancelar", batches_path, class: "bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded" %>
    </div>
  <% end %>
</div>